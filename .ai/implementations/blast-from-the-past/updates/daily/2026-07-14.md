---
type: daily-closeout
date: 2026-07-14
user: @kunalpuri-prediqt
agent: codex
duration_minutes: 517
aspects_touched: [gpu-nnps, warp-backend, particle-memory, validation-benchmarks]
---

# Daily Closeout - 2026-07-14

## Summary

Closed the remaining fp32 equation/timestep validation for the multilevel GPU
NNPS and executed ADR-0007's dense-memory decision gate. Connected refinement
is correct and faster; disconnected refinement makes dense per-level AABBs
catastrophically sparse, so ADR-0007 stays Proposed pending sparse/hybrid cells.

## Work completed

- Added fp32 3D fused WCSPH + adaptive-timestep multilevel parity.
- Measured four refinement layouts with raw timing, memory, candidates,
  accepted pairs, hardware metadata, and correctness hashes.
- Recorded the dense-only kill result in ADR/current/aspect memory and prepared
  the required prototype-owner review.
- Corrected the multilevel class residency docstring.

## Decisions made (ADRs)

- ADR-0007 remains Proposed. Dense storage is retained as the connected-level
  prototype fast path, but cannot be the universal production representation.
  Sparse keyed cells or a measured per-level hybrid are required.

## Plans approved (full + lightweight)

- Continued the approved multilevel plan (`begin`, 2026-07-06).
- Owner scope amendment, verbatim: "why fp64. we want to remain fp32 at most as
  possible". fp64 is no longer a delivery gate.

## Files changed

Host: `pysph/base/tests/test_warp_sph.py`,
`pysph/base/warp_multilevel_nnps.py`. Implementation memory: plan, ADR-0007,
current/aspect notes, experiment script + four JSON results, review, closeouts.
Boundary remains truthful.

## Tests / experiments run

- Multilevel SPH subset: `4 passed, 54 deselected`.
- Warp NNPS: `34 passed`; codegen: `10 passed`.
- Dense gate headline: large slab 29.8x fewer candidates, 19.1% lower warm
  build+fused time, dense state 49.4% of saved state.
- Kill case: two patches, dense state 26.8x saved state and 52.8x sparse
  estimate. All accepted pair totals matched the uniform grid.
- `validate-memory: PASS`; `git diff --check` and benchmark `py_compile` pass.

## Problems encountered

- The monolithic `test_warp_sph.py` process emitted passing progress but spent
  2,988s at 100% CPU in the final in-process PTX-JIT segment and was terminated.
  The isolated four-test multilevel process passes in 274.05s. This extends the
  known WSL2 module-accumulation limitation to the enlarged SPH file.

## Open questions raised

- Sorted Morton/cell keys, hash table, or per-level dense/sparse hybrid?
- What occupied-cell ratio should switch a level to sparse storage?

## Next actions

- Commit the prototype-approved evidence checkpoint; owner authorization is
  recorded verbatim in the review as "ok. commit".
- Plan/implement a sparse key/sort oracle and measured per-level hybrid before
  accepting ADR-0007 or starting runtime APR split/merge.

## Notes for next AI session

Prototype-owner approval is recorded for the reviewed checkpoint. Review:
`reviews/2026-07-14_warp-multilevel-fp32-parity-and-dense-memory-gate.md`.
Run Warp test files in isolated processes; use the fp32 multilevel subset as
the gate on this WSL2 host.
