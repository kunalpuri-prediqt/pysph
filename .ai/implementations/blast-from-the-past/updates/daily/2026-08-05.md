---
type: daily-closeout
date: 2026-08-05
user: @kunalpuri-prediqt
agent: codex
duration_minutes: 75
aspects_touched: [warp-backend, particle-memory, validation-benchmarks]
---

# Daily Closeout - 2026-08-05

## Summary

Added the missing adaptive-only variable-resolution SPH consistency path,
validated repeated split/merge transfer, tuned transfer defaults while keeping
the static refinement box, and reran the adaptive/floating comparison through
step 900. A physics validation ladder then corrected the uniform reference,
decomposed rigid impulse/torque, and tested hydrostatic and floating
equilibrium. The follow-up implemented the exact separate destination/source
gradient formulation and reran the ladder. It passes local mathematics but
fails the hydrostatic and equilibrium screens, so the implementation remains a
proposed APR path because angular, hydrostatic, coarsening, and scaling gates
still fail.

## Evidence

- Added an averaged-`HIJ` grad-`h` beta pre-pass and used the factor in adaptive
  continuity, symmetric pressure force, and equal Liu body reaction. Uniform
  mode remains on its existing equation path.
- The independent beta oracle and net-pressure-force check pass.
- Four translating split/merge/return cycles preserve mass, momentum, constant
  density, and linear hydrostatic pressure; controller suite: 16 passed.
- Coupled dam-break, grid/multilevel rigid, adaptive rebuild, and variable-`h`
  focused selection: 5 passed.
- Tuned defaults use icosa13, zero merge hysteresis, and no shifting while
  retaining the static fine box.
- Corrected step 900: finite; 3,376 fluid particles; mass drift `2.95e-9`;
  rigid geometry drift `2.28e-7`; beta range `0.2061..1.1139`; surge error
  `0.15672 m`; body-speed difference 6.95%; contact-impulse difference 22.2%.
- Step-1100 continuation remains finite. Forty-seven families partly cross the
  downstream boundary, but none has all 13 daughters outside, so the real-flow
  complete-family merge gate remains unmet.
- The earlier `dx=0.07` reference was not mass-matched. A fresh `dx=1/14`,
  `body_spacing=0.1` run has 2,744 particles and `1000.000037 kg`, matching
  the adaptive case. Against it, surge error is `0.09767 m`, height error
  `0.00213 m`, body-COM distance `0.04582 m`, and kinetic-energy difference
  5.94%. The first three pass; energy narrowly misses its 5% screen.
- Added sampled fluid/contact force and torque plus integrated linear/angular
  impulse telemetry. Adaptive versus mass-matched fine differs by 8.61% in
  fluid impulse but 58.72% in fluid angular impulse; final body angular
  velocity differs 95.21%.
- A conservative Liu coupling fixture has zero net force and torque, but the
  averaged-`HIJ` beta correction worsens manufactured interface horizontal
  residual RMS by 12.74% and hydrostatic vertical residual RMS by 26.75%.
- Floating-equilibrium screens at `dx=0.1` and `dx=1/12` improve vertically
  with refinement but retain lateral drift and a sign change. The dominant
  physics gap is pressure/surface quadrature torque, not bulk surge.
- Added opt-in generated destination/source radial gradients without changing
  uniform generated equation source. Cubic, Gaussian, and Wendland unequal-`h`
  oracles pass in fp32/fp64, as do equal-`h` reduction and 3D force/torque
  conservation tests.
- The padded interface rejects separate `h`: horizontal residual RMS is
  24.16% and vertical residual RMS 19.34% worse than uncorrected. At common
  `t=0.35 s`, equilibrium refinement halves vertical drift and strongly
  reduces rotation, but lateral drift remains and reverses sign.
- Separate-`h` step 900 remains finite with `2.97e-9` relative mass drift,
  3,388 fluid particles, no merge, and zero rigid error. Surge, height, and
  COM pass; kinetic energy differs 9.79%, angular impulse 63.15%, body angular
  velocity 82.71%, and the negative pressure tail reaches `-25.68 kPa`.
- Follow-up tests: dedicated mathematical selection 11 passed, full generator
  suite 17 passed, isolated multilevel regression passed, adaptive controller
  16 passed, and studio 19 passed.
- Final checks: focused Warp physics/rebuild selection `3 passed`; adaptive
  controller suite `16 passed`; studio suite `19 passed`; touched Python files
  compile; `validate-memory: PASS`; `git diff --check` clean.

## Remaining work

- Identify and independently test the missing mixed-resolution interface
  treatment beyond separate-`h` ownership: particle volume/partition,
  free-surface and density consistency are candidates, but should not be
  coefficient-tuned against one benchmark.
- Reduce angular-response and angular-impulse discrepancy; the mass-matched
  surge gate now passes.
- Demonstrate complete-family coarsening in the production flow and reduce the
  adaptive particle count below the matched uniform-fine case.
- Implement device-resident allocation/compaction and run formal warm GPU
  performance, memory, and matched kinetic-energy/interface comparisons.

No commit or push was requested or performed.
